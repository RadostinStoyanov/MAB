# Layout (RelativeLayout) for the help screen: back button
# two labels with information for the app and MAB problem
<HelpScreen>:
    name: "helpScreen"

   # gif animation at the bottom
    Screen:
        MyImageBackground:
            id: help_screen_background

    RelativeLayout:

        # Back button (go back to home screen)
        MyButton:
            id: back_btn_from_help
            size_hint: 0.113, 0.151
            pos_hint: {'center_x' : 0.08, 'center_y': 0.89}
            on_press: back_arrow_from_help.source = back_arrow_from_help_pressed.source
            on_release: root.manager.current  = "home"
            on_release: back_arrow_from_help.source = 'res/icon_buttons/back_arrow.png'

            #  Two images for the button: one for when pressed and one when not
            MyImageButton:
                id: back_arrow_from_help_pressed
                source: 'res/icon_buttons/back_arrow_pressed.png'

            MyImageButton:
                id: back_arrow_from_help
                source: 'res/icon_buttons/back_arrow.png'

        # BoxLayout contaioning two labels with information
        # for tha app and the MAB problem and
        # an animation
        BoxLayout:
            orientation:'vertical'

            # Label with information about the MAB problem
            Label:
                background_color: 0,0,0,0
                id:  label
                pos_hint: {'center_x' : 0.5, 'center_y': 0.5}
                size_hint: 0.9, 0.4
                font_size: 0.04 * root.height
                text:'The multi-armed bandit problem is a classic problem that well demonstrates the exploration vs exploitation dilemma. Imagine you are in a casino facing multiple slot machines and each is configured with an unknown probability of how likely you can get a reward at one play. The question is: What is the best strategy to achieve highest long-term rewards?'
                text_size:self.size
                keep_ratio: True

            # Label with information about the App
            Label:
                background_color: 0,0,0,0
                id:  label1
                pos_hint: {'center_x' : 0.5, 'center_y': 0.5}
                size_hint: 0.9, 0.3
                font_size: 0.04 * root.height
                text:'In this application you have the opportunity to test the UCB1 algorithm with Bernoulli distribution. You can enter the number of slot machines you want to do the tests for, as well as their probabilities. Once you have done that, you can choose the number of pulls and how many times the test should be played. You will see the result in a graph in the newly opened tab window.'
                text_size:self.size
                keep_ratio: True

            # Animation (3 slot machines gif)
            Image:
                source:'res/3_slots_gif.zip'
                size_hint: 1, 0.15
                pos_hint: {'center_x' : 0.5, 'center_y': 0.6}
                keep_ratio: True
                width: 50
                anim_delay: 0.135
                mipmap: True
